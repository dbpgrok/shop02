name: 🎵 Auto Shop Musique
on:
  push:
    paths:
      - 'assets/**'
      - 'index.html'
      - 'generate-shop.js'
  workflow_dispatch:

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: 20
    
    - name: Generate boutique
      run: |
        echo "Fichiers présents :"
        ls -la
        node generate-shop.js
        echo "Contenu final de index.html :"
        ls -la index.html || echo "index.html manquant"
